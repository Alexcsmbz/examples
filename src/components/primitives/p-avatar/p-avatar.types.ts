import {ReactNode} from 'react';

export type PAvatarProps = {
  src?: string | ReactNode;
  backgroundColor?: string;
  size?: string;
  offset?: string;
  mock?: string | null;
  mockImage?: ReactNode;
};
