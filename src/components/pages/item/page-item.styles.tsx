import {css} from '@emotion/css';

export const itemImage = css({
  objectFit: 'contain',
  maxHeight: '690px',
  width: '100%',
});

export const itemImageT = css({
  objectFit: 'contain',
  maxHeight: '424px',
  width: '100%',
});

export const itemImageM = css({
  objectFit: 'contain',
  maxHeight: '424px',
  width: '100%',
});
